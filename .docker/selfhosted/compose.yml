name: ezmall
services:
  ezmall:
    image: ghcr.io/x-profiler/all-in-one:${IMAGE_TAG:-latest}
    container_name: ezmall_server
    ports:
      - '${EZMALL_CONSOLE_PORT:-8443}:8443'
      - '${EZMALL_MANAGER_PORT:-8543}:8543'
      - '${EZMALL_WS_PORT:-9190}:9190'
    depends_on:
      redis:
        condition: service_healthy
      mysql:
        condition: service_healthy
    # volumes:
      # custom configurations
      # - ${CONFIG_LOCATION}:/root/.ezmall/config
    env_file:
      - .env
    environment:
      - REDIS_SERVER_HOST=redis
      - DB_SERVER_HOST=mysql
    restart: unless-stopped

  redis:
    image: redis
    env_file:
      - .env
    healthcheck:
      test: ['CMD', 'redis-cli', '--raw', 'incr', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  mysql:
    # Use MySQL 8.0 for mysql_native_password compatibility with MariaDB client
    image: mysql:8.0
    volumes:
      - ${DB_DATA_LOCATION}:/var/lib/mysql
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-}
      MYSQL_ALLOW_EMPTY_PASSWORD: yes
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
